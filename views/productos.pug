doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  link(rel='stylesheet' href='./styles.css')
  link(rel='preconnect' href='https://fonts.googleapis.com')
  link(rel='preconnect' href='https://fonts.gstatic.com' crossorigin='')
  link(href='https://fonts.googleapis.com/css2?family=Source+Sans+Pro:ital,wght@0,200;0,300;0,400;0,600;0,700;0,900;1,200;1,300;1,600;1,700;1,900&display=swap' rel='stylesheet')
  title Desafio 04
main
  .linkContainer
    a.link(href='/' target='_blank' rel='noopener noreferrer') Ingresar productos
  if showProducts
    .productsContainer
        each product in products
            .productContainer
                img.imgProduct(src=`${product.thumbnail}` alt=`${product.title}`)
                .infoProduct
                    h4.titleProduct=`${product.title}`
                    h4.priceProduct=`$ ${product.price}`
  else
    p  No posee productos para visualizar 
